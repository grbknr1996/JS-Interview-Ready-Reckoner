/**
 A binary tree is a tre where every node has atmost two children
1. A node in a binary tree can have 0,1 or atmost 2 childs
2. In a binary tree we have only 1 root
3. exactly 1 path between root and any node
 */
